# Copy to keystore.properties (gitignored) and fill in to produce a signed
# release build. Without this file, `assembleRelease` produces an unsigned APK.
storeFile=release.keystore
storePassword=your-store-password
keyAlias=bikeputer
keyPassword=your-key-password
